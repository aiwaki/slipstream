interface:
  display_name: "Slipstream"
  short_description: "Repo-specific patterns and workflows for slipstream"
  default_prompt: "Use the slipstream repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true