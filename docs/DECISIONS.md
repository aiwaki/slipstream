# Decisions

Stable decisions and invariants for Slipstream. Add entries when a rule should
survive across sessions and agents.

| Date | Decision | Status |
|---|---|---|
| 2026-07-23 | Extend frozen Windows capture v3 with capture-only v4 so a policy-classified flow retains the original client source address and port. V4 preserves every v3 passthrough decision first, rejects zero ports, unsafe source addresses, and mixed IP families, and treats this source as distinct from the outbound egress source already admitted by packet-flow v1. A separate userspace-flow-binding v1 may join capture v4 to an already-admitted frozen packet-flow-v1 capability only when generation, flow ID, transport, destination address/port, active policy, IP family, and expiry agree exactly. The binding owns no payload, instantiates no stack, and performs no Wintun, socket, adapter, route, DNS, proxy, PAC, VPN, process, service, or production-host effect. | Active |
| 2026-07-23 | Select the exact `smoltcp 0.13.1` crate only for Windows userspace-stack evaluation v1. The candidate lives in a separate Rust 1.91 crate, is a test-only dependency, and runs behind a deterministic fake Layer 3 device with fixed MTU, frame, UDP, TCP, fragmentation, and reassembly bounds. The gate proves dual-stack TCP, UDP below the IPv6 MTU, IPv4 fragmentation/reassembly, and dual-stack UDP checksum rejection. It does not link into the Windows adapter or production service host and performs no socket, adapter, route, DNS, proxy, PAC, VPN, process, or service effect. Oversized IPv6 output is an explicit fail-closed limitation and IPv6 fragment input remains unqualified. At selection time packet-flow v1 lacked the original source address and port; the later capture-v4 and userspace-flow-binding-v1 decision closes that data gap without changing frozen v1. | Active |
| 2026-07-23 | Windows packet-flow v1 consumes registry ownership so ordinary packet events copy and replace only one bounded flow; explicit inactive-generation retirement is the only full-registry operation. Capture v3 extends frozen capture v2 with the original nonzero destination port, and packet admission must preserve that port through backend opening. Packet admission consumes an opaque binding minted from the still-opening accepted data-plane session, never an independent caller-supplied session ID/request pair; the backend-open event requires a second fresh opaque capability for that same still-current session so delayed admission cannot resurrect cancellation. One capture identity, data-plane session ID, and data-plane request ID each have exactly one retained packet-flow owner until monotonic generation retirement. Undelivered backend-to-client payload makes both sustained backpressure and idle expiry client cancellation, never a fabricated backend failure. A transition is retained until every abstract effect succeeds; partial batches resume from the exact failed-command cursor and never re-reduce the event or replay the completed prefix. These are pure contract corrections only: no userspace stack, socket, packet, route, adapter, DNS, proxy, PAC, VPN, process, service, or production-host effect is admitted. | Active |
| 2026-07-22 | The first external-network-owner coexistence gate is deliberately narrower than a claim about every VPN product. A separately spawned exact integration-test child owns one uniquely named Wintun adapter, `198.18.0.2/32`, and a non-default public `/24`; the parent owns a different capture adapter and the existing production-gated destination `/32`. The child publishes an atomic ready marker only after its adapter, address, and route are live, then remains running until an explicit file-backed release. Before, during, and after exact-route activation, the parent must independently prove the child process is live and its observed adapter identity, address configuration, and broader route configuration are unchanged. Active baseline revalidation must retain the child-owned source and interface, and exact-route removal must restore ordinary selection to that same child-owned `/24`. Slipstream then removes and proves absent only its capture resources while the independent owner remains live. Only afterward may the parent release the child; the child removes its own route, address, session, and adapter, and the parent bounds their final absence. The fixture has no default route, real VPN protocol, external endpoint, backend, production-host composition, DNS, proxy, PAC, driver mutation, or broad process control. Passing it establishes coexistence with an independent VPN-like route owner, not blanket compatibility with physical, full-tunnel, split, or per-app vendor VPNs. PR #204 exact merge commit `8c1addbcfeb93f6cbd64cac07b29b98f7aae4bbb` passed native AMD64 and ARM64 plus required CI, packaged lifecycle, and audit. | Active |
| 2026-07-23 | Before selecting a Windows userspace network stack or composing packet effects into the production SCM host, freeze a versioned pure packet-to-flow forwarding contract across the existing capture, ingress, and connector boundaries. The contract must bound TCP/UDP flow ownership, backpressure, half-close/reset, timeout, route-class dispatch, and cleanup. It performs no socket, route, adapter, DNS, proxy, PAC, VPN, packet, process, or service effect and remains outside production composition until separate native and lifecycle gates pass. | Active |
| 2026-07-22 | Crash-safe active-capture removal is an independent disposable Windows gate. A parent process owns one uniquely named baseline Wintun adapter, exact source address, and non-default public `/24`; a separately spawned exact integration-test child owns the capture adapter, exact source address, and production-gated destination `/32`. The child publishes an atomic ready marker only from inside the active probe after route ownership and evidence are live. The parent independently verifies the adapter identity, address, `/32`, selected source, and selected interface, then terminates only the retained child handle. Success requires bounded absence of that adapter, address, and `/32`, followed by exact recovery to the still-live baseline interface, source, and `/24`; baseline resources are then explicitly removed and proven absent. A bounded child failsafe prevents indefinite retention if the parent disappears. The fixture has no external endpoint, backend, default route, production-host composition, DNS, proxy, PAC, VPN, broad process kill, or driver mutation. Passing it closes only abrupt process-termination cleanup; external-VPN coexistence remains independent. PR #202 and exact merge commit `ebe9f9c70b378f688badf6ba35cd96dd200d0bb4` passed native AMD64 and ARM64 plus required CI, packaged lifecycle, and audit. | Active |
| 2026-07-22 | TCP pre-existing-flow activation is an independent disposable gate rather than an inference from UDP. It uses the same two uniquely named Wintun adapters, exact private source addresses, owned non-default public `/24`, and production-tested destination `/32`, but establishes one real Windows TCP stream through a checksum-valid synthetic SYN/SYN-ACK exchange and an observed final ACK with exact sequence and acknowledgment numbers before activation. The stream must complete a baseline payload exchange before the exact route exists. Under activation, its next payload may either remain on the baseline ring and complete there, or appear on the capture ring only with the retained baseline source. Capture is treated as unsafe: the active probe fails, the owner removes only its exact route and proves baseline recovery, and the unacknowledged segment must retransmit and complete on the baseline ring. After either permitted path, the same stream must complete another bounded baseline exchange after exact-route removal. TCP sequence and acknowledgment numbers, flags, IPv4 header checksum, TCP pseudo-header checksum, endpoints, payload, and per-exchange deadlines are verified. The stream closes before route, address, session, and adapter cleanup; every acquired fixture resource still reaches explicit absence proof. This gate has no external endpoint, backend, default route, production-host composition, DNS, proxy, PAC, VPN, or driver mutation. Passing it closes only IPv4 TCP pre-existing-flow behavior; crash removal and external-VPN coexistence remain independent. | Active |
| 2026-07-22 | The first pre-existing-flow activation gate is a separate five-gate disposable IPv4 UDP qualification, not an extension of closed packet delivery. It creates two uniquely named Wintun adapters with exact private source addresses and one explicitly owned non-default public `/24` baseline route; the production-tested owner alone creates the competing destination `/32`. A connected socket must complete a checksum-valid closed request/response on the baseline ring before activation. While the exact route is active, the same socket may either remain on the baseline ring and complete there, or appear on the capture ring only with its retained baseline source. The latter is not a new capturable flow: the active probe must fail, the owner must remove only its `/32` and prove baseline restoration, and the same socket must then complete a bounded retry through the baseline ring. Any other source, path, timeout, or cleanup result fails qualification. One deadline bounds each exchange; the fixture proves absence of its broader route, both addresses, sessions, and adapters. It has no external endpoint, backend, default route, production-host composition, DNS, proxy, PAC, VPN, or driver effect. Passing it closes only the IPv4 UDP subgate; TCP, crash removal, and external-VPN coexistence remain independent. | Active |
| 2026-07-22 | A disposable exact-route activation may expose its active probe only after a second read-only baseline-egress observation constrained to the retained source address and exact interface LUID/index. The observer calls `GetBestRoute2` with those constraints, then rejects a changed returned source, interface identity, prefix, or loopback state with field-specific evidence. Copying baseline fields into a request is not fresh evidence. The observer deliberately does not apply the pure v1 source-range admission policy: a kernel-selected synthetic VPN source such as `198.18.0.0/15` remains observable, while policy admission remains a later versioned decision. Revalidation failure still invalidates the activation, removes only the owned exact route, and proves restoration before returning. This adds no socket, packet, route, adapter, resolver, backend, DNS, proxy, PAC, VPN, default-route, or production-host effect. Native AMD64 and ARM64 must qualify the constrained query under the competing exact route. Pre-existing-flow behavior remains a later independent gate. | Active |
| 2026-07-22 | Closed IPv6 packet delivery remains a separate four-gate disposable qualification, not a silent extension of the IPv4 result. It uses two uniquely named Wintun adapters with exact `/128` source addresses, one explicitly owned non-default `/64` baseline route, and the production-tested exact-route owner for only the competing destination `/128`. The capture socket binds the exact capture source; one absolute deadline covers its fixed UDP request, capture from only that adapter session, checksum-valid synthetic IPv6 response injection, and exact receive. IPv6 UDP checksum construction includes the 32-byte address pair, 32-bit upper-layer length, zero padding, and UDP next-header value. Success still requires exact-route recovery and explicit absence of the broader route, both addresses, sessions, and adapters. This fixture has no external endpoint, backend, production-host composition, DNS, proxy, PAC, VPN, or default-route effect. Passing it does not authorize forwarding, pre-existing-flow activation, crash removal, external-VPN coexistence, or production capture. | Active |
| 2026-07-21 | The first packet-delivery effect remains inside the disposable Windows fixture and requires the existing route gates plus a separate packet-delivery gate. It creates one unique Wintun adapter, one exact test address, and one exact host route, then sends one fixed IPv4 UDP payload from a socket bound to that owned address. The fixture reads only its session's Wintun receive ring, accepts only the exact source, destination, ports, lengths, and payload, builds one valid synthetic IPv4/UDP response, injects it through the same owned session, and requires the original socket to receive the exact response before a three-second deadline. Every route, address, session, and adapter is explicitly removed and proven absent. The fixture has no external server, backend, default route, production-host composition, DNS, proxy, PAC, or VPN effect. Passing it proves only a closed IPv4 capture/injection round trip; IPv6 delivery, forwarding to a real backend, pre-existing-flow safety, crash removal, and external-VPN coexistence remain separate gates. | Active |
| 2026-07-20 | The first IPv6 socket-selection candidate remains inside the disposable Windows fixture and the same three explicit CI gates as IPv4. It creates two uniquely named Wintun adapters with exact `/128` source addresses so address creation cannot implicitly own the controlled broader route. A separate test-only owner creates one fixed public IPv6 `/64` through `InitializeIpForwardEntry`, `CreateIpForwardEntry2`, `GetIpForwardEntry2`, and `DeleteIpForwardEntry2`; the production exact-route owner alone still owns the competing destination `/128`. Ordinary `GetBestRoute2` selection must move from the baseline adapter and source to the capture adapter and source under that `/128`, then return to the exact baseline identity after removal. The UDP socket sets and reads back `IPV6_UNICAST_IF` in host byte order, binds the retained baseline source, and connects without any send or receive call. The fixture explicitly removes the exact route, baseline route, both addresses, both sessions, and both adapters before accepting evidence. It creates no default route and touches no DNS, proxy, PAC, VPN, backend, production host, or remote payload. Native AMD64 and ARM64 passed on PR #194 head and the exact merge commit. | Active |
| 2026-07-20 | The first native Windows socket-selection effect is confined to the existing `disposable-windows-packet-fixture` plus three explicit CI gates enforced by the active-probe entrypoint itself; the probe-free route wrapper retains its original two-gate admission. The exact-route owner may invoke one fallible callback only after a fresh post-route observation and current opaque activation; it passes read-only destination, exact prefix, capture identity/source, and baseline egress identity/source, retains all route ownership, and runs exact cleanup plus a baseline recovery observation after callback success or failure. If the callback and recovery both fail, the recovery failure is primary while retaining the callback failure. Only the integration test may create the socket. Its IPv4 UDP socket sets `IP_UNICAST_IF` with the baseline interface index in network byte order, requires `getsockopt` to return that exact index in host byte order, binds the exact baseline source, and connects to the retained destination without calling any send or receive API. The competing route must still select the separately owned Wintun source. The fixture then proves route, address, adapter, and session cleanup. This qualifies only no-payload IPv4 socket selection; it does not prove IPv6, packet delivery, backend routing, pre-existing-flow safety, crash cleanup, VPN coexistence, or production composition. | Active |
| 2026-07-20 | The first Windows route mutation is confined to `disposable-windows-packet-fixture` and two explicit disposable-CI gates. One owner obtains the exact Wintun LUID/index, consumes a fresh opaque baseline observation, retains the complete initialized `MIB_IPFORWARD_ROW2` key for one public destination, and may call only `CreateIpForwardEntry2`, `GetIpForwardEntry2`, and `DeleteIpForwardEntry2`. It cannot create an address or default route, enumerate routes, subscribe to route changes, or touch DNS, proxy, PAC, VPN, sockets, packets, drivers, or the production SCM host. Only this owner may invoke crate-private post-route attestation. Because native AMD64 and ARM64 proved that a bare Wintun adapter cannot yield the required post-route source observation, the native fixture separately owns one exact `192.0.2.1/32` unicast row on its unique adapter through `InitializeUnicastIpAddressEntry`, `CreateUnicastIpAddressEntry`, `GetUnicastIpAddressEntry`, and `DeleteUnicastIpAddressEntry`. Mere presence is not readiness: before route qualification it must poll the exact interface/address key until the `/32` remains unchanged, `DadState` is `Preferred`, and `SkipAsSource` is false, then prove the row absent afterward. Those APIs remain forbidden in the route owner and production host. Route success requires a fresh observation selecting the exact `/32` or `/128` through the owned capture interface, a current opaque activation, explicit bounded deletion of the retained route row, activation invalidation, and restoration of the original kernel-selected interface, source, prefix, and loopback state. Any post-create failure still runs exact cleanup. Phase-specific observation failures retain the nested Win32 value as structured data. If activation and cleanup both fail, the returned error retains the structured cleanup code and Win32 value while preserving both rendered failures; route-epoch invalidation failure remains secondary to an already captured activation failure. `Drop` is best-effort compensation, never the success proof. Native AMD64 and ARM64 CI are the only effect environments. Passing this gate qualifies exact-route ownership, not socket loop avoidance, crash cleanup, VPN coexistence, or production composition. | Active |
| 2026-07-20 | Native crash qualification retains the exact `cargo` process handle instead of waiting on a PowerShell object pipeline. Output is file-backed and streamed separately, the retained process has a 120-second monotonic deadline inside a three-minute workflow ceiling, and timeout recovery requests termination only for that retained process and its descendants. Process-name discovery, broad signalling, route or adapter deletion, and any primary-workstation execution remain forbidden. This is a disposable-CI harness boundary, not production process management. | Active |
| 2026-07-20 | Windows route-observation freshness is owned by the native collector, never by its caller. The collector records `GetTickCount64` before `GetBestRoute2` and embeds that boot-relative tick inside the opaque, non-cloneable observation. The Windows-only transition entrypoint reads `GetTickCount64` again and derives the fixed five-second expiry itself; callers cannot supply observation, expiry, current, or activation timestamps. Post-route activation time likewise comes only from the second opaque kernel observation. Because Windows tick time includes sleep and hibernation, an observation cannot remain apparently fresh across suspend. Deterministic unit tests may inject a tick only through the private state-machine helper; native x64/ARM64 qualification must exercise the public collector-to-issuer path. | Active |
| 2026-07-20 | A Windows owned-route transition may become authoritative only through an opaque in-memory issuer, not through the publicly serializable v1 fixture structs. The issuer consumes one non-cloneable kernel route observation, permits only one pending exact-host transition, and binds it to one capture generation, current logical route epoch, owned capture-interface identity, destination, and exact `/32` or `/128`. It may issue a non-cloneable, non-deserializable activation token only after a second opaque kernel observation selects that exact prefix through the same capture interface within the original five-second evidence window. The serializable activation payload alone remains non-authorizing. Issuer allocation identity is checked by `Arc::ptr_eq`; another issuer or a stale token cannot pass. Cancel before any effect preserves the epoch. Any mismatch after a possible effect advances the epoch and invalidates the issuer; any later route change invalidates a pending or active token. This contract contains no Windows route API, route subscription, socket, Wintun, DNS/proxy/PAC/VPN effect, or production-host composition. Actual capture-handle ownership, exact route creation/removal, post-removal recovery, fresh source/LUID revalidation, and socket binding remain closed to a later disposable native gate. | Active |
| 2026-07-20 | Native Windows route/source observation is a read-only fact boundary, not capture or egress authorization. For one already-canonical public numeric destination, it may call only `GetBestRoute2`, canonicalize the selected source address and route prefix, and immediately round-trip the returned interface LUID and index in both directions. Unsupported families, unsafe destinations, invalid prefixes, identity churn, and a prefix that does not contain the destination fail closed with stable machine codes. The observer records the same-family source selected by Windows without equating global reachability with validity as a local or VPN source; admission policy remains in the versioned pure contract. In particular, `windows-packet-egress-v1` remains unchanged and does not yet admit `198.18.0.0/15` sources, so that common synthetic-VPN case requires a reviewed later contract version rather than a silent v1 change. The observer may not create, set, delete, or subscribe to routes; open or bind a socket; load Wintun; resolve a name; inspect or mutate DNS/proxy/PAC/VPN state; mint a route epoch; issue capture-route activation evidence; or enter the production service host. Native AMD64 and ARM64 qualification proves only that the read-only observation works on both architectures. A trusted owned-transition issuer, fresh post-activation observation, actual per-socket binding under a competing capture route, bounded removal, and explicit external-VPN coexistence remain separate gates. | Active |
| 2026-07-20 | Privileged disposable qualification must fail within its own bounded step and expose live phase evidence. A native fixture may never rely on the workflow's broad job timeout as its first escape path. The abrupt Wintun fixture bounds waiting for its exact terminated child, never blocks in `Drop`, streams test output while retaining the exact-test assertion, and gives the cleanup step a four-minute ceiling. A timeout is a visible qualification failure, not permission to broaden process matching, delete adapters or drivers, or mutate addresses, routes, DNS, proxy, PAC, or VPN state. | Active |
| 2026-07-20 | Windows packet egress v1 is a pure admission contract, not a loop-avoidance claim. Every candidate must use route evidence observed before capture and bounded to five seconds plus an exact owned capture-route activation from the baseline route epoch to the current route epoch. The transition must retain the capture generation, canonical public destination, exact `/32` or `/128` host prefix, and capture-interface LUID/index; every later route-epoch change invalidates it. The egress LUID/index and currently selected source address must exactly match the baseline; that source must remain usable and same-family, the baseline route prefix must still contain the destination, and the capture interface is always rejected. IPv4 special-purpose ranges, including the deprecated `192.88.99.0/24` 6to4 relay block, fail closed. IPv6 destinations fail closed against both the frozen 2025-10-10 IANA global-unicast allocation snapshot and the 2025-10-09 special-purpose registry; a positive allocation alone is insufficient, so assigned but non-global ORCHIDv2 `2001:20::/28`, DET `2001:30::/28`, documentation, and discard-only ranges are not public egress. IPv4 records the `IP_UNICAST_IF` interface index in network byte order; IPv6 records the `IPV6_UNICAST_IF` index in host byte order. Any system-selected non-capture interface, including an external VPN, is preserved without interface-type allowlists or mutation. The v1 module performs no route query, socket call, adapter or route effect, backend selection, or production composition. Its serializable activation evidence is not native authorization: a trusted native collector and owned-transition issuer must serialize and attest the exact change. Live LUID and source-address revalidation, actual socket binding with a competing capture route, pre-existing-flow safety, bounded removal, and external-VPN coexistence must pass on disposable AMD64 and ARM64 before this can authorize packet effects. | Active |
| 2026-07-20 | Abrupt Wintun owner cleanup is qualified separately from ordinary RAII cleanup. The disposable fixture may spawn only its exact integration-test executable, and the child must independently admit the pinned DLL before creating one uniquely named `SlipstreamCI-Crash-*` adapter and minimum session. Readiness binds the exact adapter name and child PID. The parent must prove the adapter is live, terminate only its retained child handle, wait for that exact process, and accept cleanup only after the unique name is absent within a fixed bound. Process-name matching, broad termination, adapter or driver deletion, addresses, routes, DNS, proxy, PAC, VPN, and production-host composition remain forbidden. | Active |
| 2026-07-20 | The first native Wintun effect is restricted to a feature-gated disposable Windows fixture on exact native AMD64 and ARM64 runners. It must retain the read-only admission for the exact signed DLL while loading it by absolute path with system-only dependency lookup, require a unique adapter name to be absent, create only that adapter, start and end the minimum-size session, close the creation handle, and prove the name is absent afterward. RAII cleanup runs on every ordinary error or panic. The fixture never configures addresses or routes, calls `WintunDeleteDriver`, touches DNS/proxy/PAC/VPN, or enters the production service host. This proves only adapter/session lifecycle compatibility; it does not authorize capture, routing, crash recovery, coexistence, packaging, or production composition. | Active |
| 2026-07-20 | Every build or qualification that embeds Geph must derive the immutable tag from both `vendor/geph/VERSION` and `vendor/geph/SOURCE.json.release_revision`. An unrevisioned or superseded release is not equivalent evidence even when the crate version matches. Release downloads use one bounded retry helper, stage into a fresh temporary directory, and publish output only after the complete requested asset set arrives; an exhausted download remains a visible infrastructure failure. A packaged lifecycle run qualifies only the exact Geph binary embedded by the release path. | Active |
| 2026-07-20 | Windows exact-route packet adapter v1 remains frozen as non-authorizing research. A complete destination-binding issuer cannot be claimed from read-only system DNS while policies cover unbounded subdomains, applications may use encrypted DNS, and Wintun exposes L3 packets without accepted-stream hostname context. No DLL loading, adapter creation, route mutation, or production composition may proceed under that premise. The separate capture-only v2 contract now reclassifies each observed flow from bounded transport-matched TLS SNI or QUIC Initial evidence. An exact route may widen capture but never authorize a backend; direct, unknown, missing, ECH, malformed, stale, unsafe, and mismatched observations preserve direct passthrough. V2 is pure and not composed into production. Disposable qualification must still prove outbound loop avoidance, activation safety for existing flows, prompt expiry/removal, crash rollback, and coexistence with external VPNs on AMD64 and ARM64 before any native effect is admitted. | Active |
| 2026-07-20 | Startup safety must become observable before any network-dependent qualification begins. After the owned loopback listener starts, the daemon publishes a fresh probe-free `dormant` StatusV2 snapshot before probing Geph, DNS, HTTPS, or PF. Regular status publication consumes only cached DNS diagnostics; the background refresh is bounded independently. Every system-DNS baseline lookup runs as the console user in a separate killable child process with a per-host timeout, and the complete pre-PF qualification has one monotonic budget. A stalled resolver is skipped; it cannot block later baseline targets, status publication, shutdown, or install rollback. Failed or exhausted qualification leaves the private anchor absent and retries later without changing system DNS. | Active |
| 2026-07-20 | The primary workstation is not Slipstream's first privileged integration environment. A candidate must first pass unit and real-process regression tests, the disposable packaged lifecycle gate, and a targeted reproduction of the incident class. Workstation validation uses one exact CI artifact, a prepared bounded rollback, and a short user-scheduled window; it must not require the user to remain available while builds or ordinary tests run. No unattended reinstall, re-arm, password prompt, or repeated workstation trial is permitted after a failed baseline. | Active |
| 2026-07-19 | On current macOS, `lo0 (skip)` is kernel PF interface state and may be present even when `/etc/pf.conf` contains no `set skip on lo0`. A private `route-to` plus `rdr on lo0` topology cannot receive redirected traffic while that flag remains set. Slipstream may clear only `PFI_IFLAG_SKIP` through the exact `DIOCCLRIFFLAG` ioctl, only after its private rules are loaded and a root-owned `0600` lease has been atomically written and fsynced. The redirect excludes `127.0.0.0/8`, and the active monitor revalidates live loopback visibility even while the child anchor remains loaded. Teardown, recovery, failed arm, install, reinstall, and uninstall flush only `com.apple/slipstream`, restore the leased flag with exact readback, remove the lease, and only then release the owned `pfctl -E` token. A loaded `KeepAlive` job is never signalled before `bootout` is proven successful or absent, and cleanup repeats after every verified survivor is quiescent. Reinstall does not replace its runtime until that quiescence is proven. Slipstream never uses `pfctl -d`, a global ruleset reload, `pfctl -F all`, or Control-D to obtain loopback visibility. | Active |
| 2026-07-19 | The macOS private PF anchor may become active only inside a bounded baseline-qualification transaction. Before arming, the original console user must complete verified TLS and receive a syntactically valid HTTP response through the current system route. After arming, Slipstream replays only the exact hostname, path, and numeric destinations proven before PF; it does not re-resolve them or change DNS, proxy, PAC, VPN, or policy. A failed post-arm proof clears only `com.apple/slipstream`, releases only Slipstream's PF token, and blocks automatic re-arm until a real wake or network change. If that cleanup itself fails, the listener and runtime remain alive while the daemon retries its own anchor; uninstall/install rollback must not force-stop the listener or delete its runtime until PF cleanup succeeds. Shutdown clears the private anchor before closing the listener. Uninstall first disables the launchd label and clears the private anchor, then boots out the `KeepAlive` job before signalling any surviving verified PID; an already-exited old PID is success, not a cleanup failure. | Active |
| 2026-07-19 | The local transparent engine is independent of optional geo-exit backends. A clean install arms only `com.apple/slipstream` once its listener, local capacity, parent anchor, and conflict checks are ready; Discord and YouTube remain local-bypass and never select Geph. No route assumes that owned Geph, external VPN, Smart DNS, ordinary DNS, or any particular combination is present: candidates are admitted only from observed capability and policy. A reviewed geo-exit host may use proven Smart DNS or verified owned Geph, but absence or cooldown of those app-owned backends cannot make all local routing dormant. The fallback immediately relays the exact destination chosen before PF redirection, without alternate DNS, desync, or a server-first payload gate. A full-tunnel VPN that owns the default `utun*` route keeps Slipstream dormant rather than being intercepted. Split/per-app VPN equivalence is not assumed without separate qualification. A Geph failure cools only Geph. A zero-byte close after a Geph relay has consumed the stream cannot replay that connection, but the next client retry may use the preserved system destination. External DNS, VPN, proxy, and PAC remain read-only. | Active |
| 2026-07-19 | Global TCP/443 capture must preserve a byte-transparent baseline for direct, unknown, non-TLS, and TLS-without-SNI traffic. The first connection uses only the numeric destination selected before PF redirection and starts bidirectional relay immediately; it does not resolve the hostname again, select a desync strategy, wait for the first server payload, or enter a persistent backend circuit. An orderly client EOF is propagated as a bounded TCP half-close so a delayed server response is not discarded. A failed or stalled unknown stream, including a server-first close with zero downstream bytes, may schedule only an exact-host local recovery for a later client retry. The already-sent first flight is never replayed through a second route, and no such failure can promote a host to Geph. | Active |
| 2026-07-18 | An exact Windows `/32` or `/128` route may proceed past the pure shared-destination gate only with opaque evidence from the future owner of a complete resolution boundary. A partial DNS cache or absence of a known conflict is not proof. Evidence is bounded to 256 canonical, strictly sorted, unique policy hostnames and 30 seconds; it binds one exact destination, includes the candidate host, and every binding must reclassify under the active tables to the same route class and strategy set. Direct, local-bypass, and geo-exit conflicts fail closed. The admission expires at the earlier route/evidence deadline and carries a nonzero collector generation. Any future native route must retain that exact generation lease for its entire lifetime and be removed before the lease is released. The pure admission is not native authorization, no native issuer exists yet, and the production host remains no-network. | Superseded by the 2026-07-20 capture-only v2 decision |
| 2026-07-18 | Native Wintun artifact evidence is collected read-only and offline before any packet capability exists. Slipstream opens the staged archive, license, and architecture DLL as bounded non-reparse regular files, hashes the exact handles, parses the PE machine, and passes the still-open DLL handle to `WinVerifyTrust`. Trust verification suppresses UI and network retrieval, requires Windows Authenticode success, extracts the exact certificate organization and leaf SHA-256, and requires a verified timestamp countersigner. Exact reviewed artifact hashes remain the immutable identity; online revocation is not a hidden runtime dependency. A successful native admission is opaque, non-cloneable, and retains the read-only DLL handle so the path cannot be replaced before a separately reviewed loader consumes it. This collector cannot load the DLL, create an adapter, install a route, invoke a process, or inspect or change DNS/proxy/PAC/VPN state, and it is not composed into the production service host. | Active |
| 2026-07-18 | Slipstream will not ship or sign its own Windows kernel driver. Microsoft production driver submission requires organization-backed Hardware Dev Center access and an EV certificate, while test signing changes the user's boot security posture. The Windows candidate is therefore the unmodified official prebuilt and signed Wintun 0.14.1 DLL for AMD64/ARM64, admitted only after exact archive, license, DLL, PE-machine, Authenticode publisher, signer, and timestamp evidence. Wintun is only a local L3 packet interface; using it does not add the WireGuard protocol, a WireGuard tunnel, or a WireGuard endpoint. It is not an accepted-TCP-stream source, so the WFP capture/runtime/session v1 contracts remain frozen research artifacts and are not composed into production. `windows-packet-adapter-v1` produces only non-authorizing, fresh public `/32` or `/128` candidate plans after active-policy reclassification and an opaque, non-deserializable resolver capability that binds the same canonical host to the selected address. IPv6 is conservatively limited to reviewed global-unicast space outside IANA special-purpose ranges. Its shared-destination gate remains a frozen pure fixture; the 2026-07-20 decision forbids building native route authorization from that incomplete DNS premise. DLL loading, adapter and route ownership, userspace packet-stack integration, rollback, and disposable coexistence qualification remain closed. No default route or system DNS/proxy/PAC/VPN mutation is permitted. | Active |
| 2026-07-18 | Windows WFP management session v1 uses seven fixed owned keys and a separate fixed 128-byte provider context binding service, runtime, session, target PID, executable hash, exact IPv4/IPv6 loopback listeners, and TCP/443 scope. The controller admits the atomic session only after non-replaceable same-binding kernel-registration and listener-readiness proofs. One dynamic engine transaction adds the non-persistent provider, sublayer, V4/V6 management callouts, general provider context, and terminating filters in exact order; any partial failure invalidates earlier absence evidence, aborts, and closes rather than deleting objects by key. Session close precedes an observer-session lookup of both exact filter keys, and the absence proof must match the last attempted session generation; retained filters keep listener and kernel proofs live. Windows CI qualifies only an empty dynamic begin/abort/close and filter-absence probe until a disposable kernel callout exists; no terminating filter is committed against an unavailable callout. The production SCM host remains no-network and external DNS/proxy/PAC/VPN state is untouched. Any key or provider-context layout change requires v2. | Frozen research |
| 2026-07-18 | Windows WFP runtime lifecycle v1 is a pure, effect-injected state machine bound to the exact owned service generation, capture instance, and reducer-issued monotonic runtime attempt. Kernel callouts register before exact listener readiness; provider, sublayer, non-persistent management callouts, provider context, and V4/V6 filters enter through one failure-atomic dynamic-session command. A stop or runtime fault emits dynamic-session close as its sole first action. Exact owned-filter absence is required before listener stop, bounded accepted-stream drain, or kernel unregister. If either filter remains, the listener and kernel callouts stay live and the reducer schedules another proof instead of creating an unavailable-callout blackhole. An accepted stream above the configured bound receives an exact retryable reject effect and never enters runtime state. Drain timeout force-closes the exact retained stream set before unregister, and interrupted effect batches resume from a non-replaying cursor. Stale attempts, sessions, completions, and timers are rejected. This contract invokes no WFP, Winsock, driver, socket, DNS, proxy, PAC, or VPN API; direct connector v1 and the production no-network service host remain unchanged. | Frozen research |
| 2026-07-18 | Windows WFP redirect context v1 is a fixed 128-byte append-only wire format, not a native effect. It binds TCP address family and original remote/local endpoints to the exact owned service generation, target PID, nonzero 128-bit capture instance, and nonzero executable SHA-256. Lengths, flags, IPv4 padding, reserved bytes, numeric endpoints including IPv4-mapped IPv6 safety, exact loopback listener, and opaque redirect-record size are validated before a capture can enter the technology-neutral source. The validated capture also carries the monotonic source-issued connection ID. Handoff revalidates a complete direct-ingress request against active policy and requires the same connection identity and endpoint, so concurrent connections to one address cannot exchange admissions. Capture and redirect-record plans are non-cloneable; mismatch returns ownership intact, and the opaque ingress/connect plan is exposed only after redirect records are explicitly marked applied. Direct connector v1 remains frozen. The production service host, WFP engine, filters, driver, sockets, and external DNS/proxy/PAC/VPN state are untouched. Any wire-layout change requires v2. | Frozen research |
| 2026-07-18 | Native Windows TCP capture v1 uses a minimal WFP callout at `FWPS_LAYER_ALE_CONNECT_REDIRECT_V4/V6` plus the exact owned Rust service. The driver first registers the kernel classify callout with `FwpsCalloutRegister`; only then may one explicit dynamic-session transaction add the non-persistent, non-boot provider, sublayer, `FWPM_CALLOUT` management object, provider context, and filters after listener and service/driver identity are ready. Closing that session is the first stop and crash fail-safe so filters and their management callout disappear before accepted streams drain; the kernel callout is unregistered and the driver becomes unloadable only after exact filter absence is proven. This ordering prevents a surviving terminating filter from blocking traffic because its callout is unavailable. The callout redirects only to the owned loopback listener, sets its exact target PID, preserves bounded versioned original-destination context, and uses `FwpsQueryConnectionRedirectState` to avoid self-redirection loops while respecting other WFP redirectors. The service rejects ordinary loopback sockets without valid WFP context, queries the opaque redirect records from every accepted socket, and must attach those records to each outbound socket before bind/connect. Direct connector v1 remains frozen; redirect-record propagation enters a separate v2 or WFP-specific connector boundary. WinDivert, system proxy/PAC, and TUN are not capture-v1 implementations. The production SCM host remains no-network until disposable x64 and ARM64 qualification proves exact ownership, crash/reboot fail-open behavior, bounded stop, coexistence, uninstall, and signed-driver packaging. | Superseded |
| 2026-07-18 | Windows capture source v1 is a technology-neutral ownership contract, not an interception implementation. A native adapter may register one accepted client stream under a one-shot opaque resource token and report only its canonical numeric `original_destination`. The source allocates a monotonic connection ID, retains ownership while an external authority performs route admission, and builds direct-ingress evidence only from the resulting exact request/session identity. It cannot resolve names, classify hosts, choose a route or backend, or inspect or mutate DNS/proxy/PAC/VPN state. Absolute connector and first-payload deadlines are checked against admission time and are never rebased. A duplicate token fails without closing or replacing the already-tracked stream. Invalid, rejected, expired, startup-racing, or shutdown-racing captures close exactly once. Handoff is failure-atomic: on failure the resource remains source-owned and can be retried or explicitly closed. Stop prevents new admission before closing unhanded resources and has a bounded force-stop deadline; late readiness or capture cannot resurrect a terminal source. Effect batches expose exact non-replaying resume cursors and terminal history is bounded. The production SCM host does not import this module and remains no-network until a separately reviewed native source satisfies this contract on disposable Windows. | Active |
| 2026-07-18 | Windows direct ingress v1 binds one non-cloneable adapter-owned client TCP stream to fresh `original_destination` evidence, the exact reducer-issued session and request IDs, and the same canonical numeric endpoint already admitted by direct connector v1. Connector preload is forbidden, so client bytes cannot be invented outside the owned stream. Backend payload reaches the data-plane reducer only after full client-socket delivery, and the first-payload deadline remains active until that delivery. Client EOF, read failure, or downstream backpressure cancels without manufacturing a backend failure; bounded upstream backpressure is an explicit backend reset. Backpressure measures a no-progress interval, not total transfer time. Client reads, connector reads, channels, internal connector buffering, cancellation bookkeeping, retained state, and both backpressure intervals are bounded, and startup failure returns the owned staged stream for non-replaying effect recovery. Exact deadline and progress boundaries are qualified through deterministic relay-state tests; native loopback qualification must not depend on platform TCP buffer sizing or autotuning. The module cannot resolve names, select a route, or inspect or mutate DNS/proxy/PAC/VPN state. The production SCM host remains no-network until a separate reviewed Windows interception source can create this stream and evidence. | Active |
| 2026-07-18 | Windows direct connector v1 is the first native data-plane primitive and is limited to one already-admitted `direct` session plus one canonical numeric IPv4/IPv6 endpoint. Its opaque plan is created only after reclassification through the active policy tables; hostname resolution, route selection, local bypass, Smart DNS, Geph, and reads or mutations of external DNS/proxy/PAC/VPN state are outside the module. Payloads, queues, read chunks, connect time, and first-payload time are bounded. One worker owns its TCP stream until clean close, reset, caller cancellation, deadline, or shutdown and returns events with the reducer-issued session identity. The native effect stages a validated plan before `StartSession`, accepts no other backend, closes before outcome, and bounds retained state. Loopback CI must qualify connect, first payload through the real reducer/effect chain, reset after partial payload, cancellation, deadline, and shutdown. The production SCM host remains no-network until a separate reviewed ingress can supply numeric endpoint evidence and an adapter-owned client stream. | Active |
| 2026-07-18 | Windows worker-host composition v1 is a pure ordering boundary around the frozen SCM host and data-plane reducers. SCM `RUNNING` may follow only a committed worker-ready effect. Stop and system shutdown are host-owned: `STOP_PENDING` is reported before cancellation, bounded worker drain or deadline completes next, and `STOPPED` may follow only `ReportWorkerStopped`. Startup failure reports `STOPPED` with a nonzero exit code, and inconsistent host/worker phase pairs fail closed. Late backend completion cannot resurrect either a session or the service, and an interrupted mixed SCM/worker effect batch resumes from an exact cursor without replaying its committed prefix. The production host uses an injected no-network effect; this decision adds no socket, DNS, proxy, PAC, VPN, process, or packet API. | Active |
| 2026-07-18 | Windows data-plane v1 is frozen as a pure worker/session reducer plus an injected recording effect before any native network API is admitted. Every request carries a complete `slipstream-core` policy result and an explicit backend, but caller-supplied policy metadata is untrusted: the reducer reclassifies the normalized host through the active validated policy tables before admitting a session. Protected Discord and YouTube traffic accepts only `local_bypass` with `local_engine`, including when a caller labels the host `generic`; route class, strategy set, backend, or active-classification mismatches are rejected without stopping the worker. First payload proves backend readiness but is not terminal stream success: a later reset after partial payload remains a normalized `stream` failure. Caller and shutdown cancellation are not backend failures. Adapter-owned resources remain owned until cancellation acknowledgement or a bounded deadline and close exactly once before any outcome. Each effect command is failure-atomic; a multi-command transition commits reducer state only after all effects complete, and an interrupted batch exposes the exact retry cursor so completed effects are never replayed. Monotonic session IDs and bounded deterministic terminal retention prevent both stale-event ABA and unbounded state growth; late completions cannot resurrect terminal sessions. External DNS, proxy, PAC, and VPN state remains untouched. | Active |
| 2026-07-18 | The production Windows service host is the same content-addressed executable that manages its lifecycle. Exact `--service` is its only SCM mode; all controller operations require explicit `manage install|start|stop|recover|uninstall`, and install accepts only a positive generation, hashes `current_exe`, then relies on the existing payload transaction to reopen and independently verify it. SCM status follows the bounded `START_PENDING -> RUNNING -> STOP_PENDING -> STOPPED` sequence and accepts both stop and shutdown controls. The host may compose the pure data-plane state machine but contains no native data-plane effect, socket, DNS, proxy, PAC, VPN, packet, process-discovery, or process-termination surface. Disposable Windows CI must invoke management through separate processes and prove idempotent install/stop/start/uninstall plus PID replacement after restart before a native network API may be considered. | Active |
| 2026-07-18 | The production-facing Windows service controller acquires the shared machine-wide operation lock before collecting durable lifecycle state or live SCM/ownership evidence and holds that same lock through reconstruction, reducer execution, and every native effect. Only a committed active-install record plus exact live owned identity becomes actionable. Fresh or terminal exact absence remains idempotent; reinstalling the identical already-running service is a no-op. Interrupted, inaccessible, inconsistent, foreign, or unknown evidence remains non-mutating. Disposable CI must prove a failed restart persists its bounded attempt and a later controller process resumes recovery before this boundary may feed a production host or any Windows networking effect. | Active |
| 2026-07-17 | The native Windows lifecycle is composed behind one shared operation-lock acquisition per action; filesystem and SCM sub-effects expose lock-free internal entry points only for that compositor. Final cleanup is ordered by evidence, not by request acceptance: unregister closes the exact service and manager handles, then waits boundedly until `dev.slipstream.service` is truly absent before the owned executable may be removed. A post-commit failure preserves the exact identity in the absent tombstone and defers active-install clearing until both exact SCM absence and exact payload-path absence are proven. Unknown, foreign, inaccessible, or conflicting evidence remains non-mutating. Disposable CI must run a real service through install, stop, start, crash recovery, uninstall, and post-commit compensation before any Windows networking effect is admitted. | Active |
| 2026-07-17 | Every privileged Windows service lifecycle, staged-payload, and SCM effect acquires the same machine-wide v1 operation lock before reading authorization evidence and holds it through commit, compensation, or native mutation. The lock is bounded, non-inheritable, and created with a LocalSystem/built-in Administrators DACL. Because Windows ignores supplied security attributes when a named object already exists, every returned kernel-object handle is independently checked for a trusted owner and allow-only trusted writer/waiter principals before use; squatted, wrong-type, inaccessible, or permissive objects fail closed. Abandoned ownership is safe only because each effect revalidates strict durable evidence before acting. An accepted stop request must keep the same exact service handle and wait to a bounded, identity-matching `Stopped` state before unregister may run. Timeout, unexpected state, or changed evidence fails closed. | Active |
| 2026-07-17 | Native Windows SCM mutation is isolated behind a pure action-specific v1 gate. Register requires exact absence, zero-attempt running intent, no active-install record, and exact staged payload. Start, stop, and unregister require compatible durable intent, a matching-or-missing active record, exact staged payload, and same-handle stable ownership proof; an absent install-rollback tombstone may authorize only cleanup. Each native action opens only `dev.slipstream.service` with its single mutation right plus query rights, never enumerates or reconfigures services, and contains no process, DNS, proxy, PAC, VPN, socket, or packet API. Foreign, transitional, unknown, or mismatched evidence performs no mutation. | Active |
| 2026-07-17 | Durable Windows service state is a separate filesystem-only transaction. Strict bounded v1 intent and active-install records live under the protected machine directory and use owner-only, exact non-reparse handles. A pending file is registered before write, flushed, verified from the same handle, and committed with same-volume write-through rename. Any pending, inaccessible, invalid, permissive, orphaned, or identity-mismatched evidence blocks future SCM mutation. Stable state is only a prerequisite for evaluating a separate action-specific ownership gate, never authorization by itself. Active-install commit requires matching zero-attempt running intent plus exact owned staged-payload evidence; clearing it requires and preserves an absent intent tombstone and verifies disappearance. The module contains no SCM, process-control, DNS, proxy, PAC, VPN, socket, or packet API. | Active |
| 2026-07-17 | Packaged Safari lifecycle cleanup treats both zombie state (`Z`) and macOS process-exit state (`E`) as terminal observations. Once `ps` reports a same-UID Safari PID as exiting, the harness waits for natural removal and never tries to re-identify or signal it from an unavailable `(Safari)` command line. All non-terminal process control still requires the exact Safari executable identity. | Active |
| 2026-07-17 | Native Windows payload staging is a separate filesystem-only effect for `StagePayload` and `RemoveOwnedPayload`. The source, pending executable, final executable, pending owner record, and final owner record are opened as non-reparse handles and bounded before use. The source SHA-256 must equal the lifecycle identity. New directories and files use a protected LocalSystem/built-in Administrators DACL; each pending handle is registered for compensation before fallible I/O, the executable is flushed and committed first, and the strict owner record is the final commit marker. Success requires staging-only read evidence to reopen both and reproduce the exact identity while the transaction still holds its write/delete handles; this only widens sharing, grants no write access, and does not loosen the normal ownership collector. Failure and removal mark only already-proven handles for deletion; foreign, permissive, ambiguous, or colliding paths are never replaced. This module has no SCM, process-control, DNS, proxy, PAC, VPN, socket, or packet API. | Active |
| 2026-07-17 | Windows ownership evidence is collected from handles, never trusted path strings. The v1 machine record lives under the system `FOLDERID_ProgramData` known folder with no environment-variable fallback. The collector requires an exact final DOS path, a non-reparse regular file, a LocalSystem or built-in Administrators owner, no write-capable allow ACE for any other principal, bounded strict JSON, and a same-handle executable SHA-256. Missing, inaccessible, malformed, complex, or permissive evidence remains non-owned. The collector has no write, service-control, process, DNS, proxy, VPN, socket, or packet API. | Active |
| 2026-07-17 | Windows service ownership is a separate pure v1 proof contract. `Owned` requires an owner-only machine record whose exact service name, canonical SCM command, absolute executable path, lowercase SHA-256, and positive generation agree with a stable read-only SCM observation and independently verified executable evidence. Missing, inaccessible, malformed, permissively writable, or transitional evidence is `Unknown`; path, command, name, or hash mismatch is `Foreign`. Neither JSON content nor service presence can claim ownership by itself. This contract performs no filesystem, ACL, process, SCM, DNS, proxy, VPN, socket, or packet effect. | Active |
| 2026-07-17 | Native Windows service discovery begins with a query-only SCM observer isolated from lifecycle decisions. It opens only the exact `dev.slipstream.service` name and may read status and configured binary command. Only `ERROR_SERVICE_DOES_NOT_EXIST` proves absence; access errors, malformed data, transitional states, and unknown values never become absent or owned. A present service remains unowned evidence until a separate owner-only record, expected path, executable hash, and generation agree. This observer cannot create, start, stop, delete, execute, or reconfigure a service and cannot touch DNS, proxy, PAC, VPN, sockets, or packets. | Active |
| 2026-07-17 | Windows service lifecycle v1 is a separate effect-injected contract before native service-manager code. Every durable intent is bound to the exact `dev.slipstream.service` identity, executable SHA-256, generation, and crash-attempt count. Install commits only after readiness and compensates to a verified absence. Stop and uninstall persist non-running intent before service actions; `absent` intent cannot be weakened by start or stop, and clearing the active install record must preserve that tombstone. Crash recovery runs only for exact owned state with `running` intent, persists the bounded attempt before restart, and resets it only after readiness. Cleanup may tolerate intermediate errors only when the final state is proven; failed compensation becomes unknown and never auto-recovers. Foreign or unknown services receive no destructive effect. | Active |
| 2026-07-17 | Windows is the first platform adapter, but its v1 boundary is deliberately no-network and effect-injected. It must execute the shared routing, recovery, StatusV2, signed-bundle, and activation contracts with deterministic fake effects before native service, process, filesystem, DNS, proxy, VPN, socket, packet, or installer code is admitted. Rollback durable commit and runtime activation are one explicit atomic effect: failure leaves the current policy active. Native lifecycle work must retain this harness as a regression boundary and qualify install, start, stop, crash recovery, and rollback on disposable Windows before routing effects are introduced. | Active |
| 2026-07-17 | Every Python runtime path that activates or rolls back a signed route policy goes through activation contract v1. The adapter executes reducer actions in order, keeps candidate and rollback file changes transactional, restores the prior runtime policy on effect failure, and records every consumed trial generation in an owner-only activation file before candidate activation or health checks. Successful commits mirror that generation in backward-compatible policy metadata. Persisted signed provenance remains explicit even when its content has the bundled SHA-256; a newly verified candidate whose canonical SHA-256 already equals the active policy remains the frozen v1 `no_change` case and creates no trial. Direct signed apply/save bypasses are removed. The remote channel remains disabled, no production key is added, and this migration changes no PF, DNS, proxy, PAC, VPN, or route classification behavior. | Active |
| 2026-07-17 | Signed route-policy activation contract v1 is a pure compare-and-swap reducer shared by Python and `slipstream-core`. Only an already-verified signed identity may enter one trial at a time. Trial start and rollback require the expected active SHA-256; health results require both the exact candidate SHA-256 and the reducer-issued monotonic trial generation, so an aborted attempt cannot authorize a retry of identical content. A candidate commits only after a completed gate with at least one success and zero degraded or blocked checks. Rejection restores the stable active policy, rollback consumes one previous slot before falling back to bundled policy, and adapter actions are ordered data only. This extraction adds no fetch, persistence, trust key, runtime apply, PF, DNS, proxy, PAC, VPN, or routing behavior. | Active |
| 2026-07-16 | Signed route-policy bundle contract v1 is shared by pure Python and `slipstream-core`. Ed25519 covers the normalized manifest's Python-compatible canonical JSON bytes; an optional lowercase SHA-256 is checked independently. Envelope and nested manifest failures have stable codes and paths. The fixture key is test-only: this extraction adds no production trust key, remote fetch, automatic apply, filesystem, or network behavior. | Active |
| 2026-07-16 | Daemon shutdown is a one-way lifecycle transition. It blocks new status publication before cleanup and serializes the final removal of both `/var/run/slipstream.status` and its temporary file with any in-flight atomic writer. Once teardown begins, a monitor thread cannot recreate public status after the private PF anchor has been cleared. | Active |
| 2026-07-16 | Route-policy manifest contract v1 is shared by pure Python and `slipstream-core` validators with structured error codes and bounded inputs. Validation protects every bundled Discord/YouTube local-bypass and Google/Spotify direct-first suffix by its effective first-match result, including explicitly listed subdomains inside each protected family, not merely by finding a later group entry. `geo_exit` is forbidden in `static_routes`, and geo-exit suffixes cannot overlap protected families. This extraction does not enable remote policy, add trust keys, switch the runtime policy channel, or perform network/OS work. | Active |
| 2026-07-16 | The privacy-bounded StatusV2 model is shared through `slipstream-core`. Its language-neutral fixture freezes the existing tray projection, partial transition payloads remain valid, and unknown additive fields survive round trips. The transition keeps V1 readable and does not change daemon output, tray copy, or runtime routing. Public status still excludes hostnames, IP addresses, secrets, raw failures, and strategy details. | Active |
| 2026-07-16 | Rust routing classification and recovery reduction are frozen by the same language-neutral v1 vectors as Python. The classifier consumes validated tables; the reducer returns ordered data-only actions and gives protected Discord/YouTube groups local recovery even if an adapter misclassifies them. Adding these core APIs does not switch the macOS runtime adapter. | Active |
| 2026-07-16 | Shared routing state machines live in the pure `crates/slipstream-core` crate. The core accepts addresses, times, and outcomes as data and owns no sockets, clocks, filesystem, process, Tauri, or OS calls; adapters own those effects. Frozen language-neutral contract tests move with the core, while the tray may re-export modules only for transition compatibility. | Active |
| 2026-07-16 | After automatic context compaction or task resume, agents must verify the current worktree, merged/open PRs, required CI, and `docs/CURRENT_STATE.md` before choosing work. Conversation summaries and milestone labels are navigation only, never completion evidence. A PR that changes the next verified action or milestone status updates the checkpoint in the same change. | Active |
| 2026-07-07 | Discord traffic stays on local bypass/desync, including `updates.discord.com` and `gateway.discord.gg`. It must not route through Geph. | Active |
| 2026-07-07 | YouTube/googlevideo video delivery stays on local bypass. Geph is not the fallback for video playback. | Active |
| 2026-07-07 | QUIC/UDP must not be blocked globally. Any future QUIC handling must be scoped to verified host/IP evidence and a concrete failure mode. | Active |
| 2026-07-08 | Geph is reserved for geo-exit cases where the service rejects the user's Russian IP address. | Active |
| 2026-07-13 | Unknown hosts must never auto-promote to Geph from local stalls plus a successful tunnel payload: that only proves that Geph works, not that a foreign exit is required. Geo-exit is selected only by explicit, reviewed policy. | Active |
| 2026-07-13 | `google.com` and the Spotify delivery families (`spotify.com`, `spotifycdn.com`, `scdn.co`) use a local-only `direct_first` ladder: plain TLS is always tried first, then bounded local desync if necessary; Geph is never an edge. A cached local fallback cannot displace the next direct attempt. | Active |
| 2026-07-08 | Slipstream must not mutate external DNS, VPN, PAC, or proxy settings. It may detect and warn about them. | Active |
| 2026-07-08 | Repo docs are the primary source of project knowledge. Codex memory stores durable agent/user preferences and pointers, not long investigations. | Active |
| 2026-07-09 | GitHub developer/download endpoints use direct passthrough and plain TLS; they should not route through Geph or the generic desync ladder. | Active |
| 2026-07-09 | Human-facing runtime logs should avoid `failed` for expected transient retry/fallback events; reserve alarming wording for action-required errors. | Active |
| 2026-07-10 | Slipstream owns only the `com.apple/slipstream` PF anchor. It must not load Slipstream rules into the global ruleset, edit `/etc/pf.conf`, or disable PF globally. | Active |
| 2026-07-16 | A root PF listing that resembles a pre-anchor Slipstream redirect is not ownership proof. Detection is read-only: the daemon disables only its own launchd label, clears only its private anchor/token, reports the conflict, and exits. Startup, recovery, upgrade, and uninstall never reload `/etc/pf.conf` automatically. | Active |
| 2026-07-16 | A terminal startup `conflict` remains visible after the normal 15-second live-status TTL. Successful startup replaces it and uninstall removes the status file; stale live states still become `off`. | Active |
| 2026-07-10 | PF enablement is reference-counted with the token returned by `pfctl -E`; cleanup flushes only the private anchor and releases only that token. | Active |
| 2026-07-10 | The bundled Geph listener on `127.0.0.1:9954` is usable only when PID, executable, config path, and listener ownership agree. An unknown listener is a fail-closed conflict. | Active |
| 2026-07-10 | A separately managed Geph listener on `127.0.0.1:9909` is diagnostic-only unless the user explicitly opts into that port. Slipstream never stops it. | Active |
| 2026-07-10 | Geph configuration directories are owner-only (`0700`); secret-bearing config, cache, and ownership files are `0600` and written atomically. | Active |
| 2026-07-16 | A legacy plaintext Geph secret is removed only after the exact Slipstream Keychain write succeeds. A failed migration retains the source value for retry; a failed manual account update does not restart the owned Geph job as if it succeeded. A later successful Keychain read retries removal of any stale plaintext copy. | Active |
| 2026-07-10 | A transparent HTTPS PF redirect loaded before `com.apple/*` owns the real traffic path because PF uses the first matching translation. Slipstream must pause, report the conflicting anchor, and recover automatically when it disappears; it must not flush, stop, or rewrite the other product. | Active |
| 2026-07-10 | Runtime recovery is selected by a pure reducer. Discord/YouTube outcomes can produce only local strategy invalidation, exact-host re-sweep, and recheck; only a verified owned Geph backend can produce a restart action; external state produces a warning only. | Active |
| 2026-07-11 | Transparent PF interception is armed only after the local listener and the enabled geo-exit backend have a verified usable port. A runtime geo-exit failure pauses only `com.apple/slipstream`; retries use the native path until a bounded hold expires and backend readiness is re-established. | Superseded 2026-07-19: local routing no longer depends on a geo-exit backend |
| 2026-07-11 | Geph health hysteresis may preserve a previously verified port, but it must never invent an `up` state on cold start. Endpoint failures do not let the tray restart a live Geph process; LaunchAgent `KeepAlive` handles process death, and any future live-process restart must be daemon-coordinated after routing is idle. | Active |
| 2026-07-18 | `Quit Slipstream` keeps its non-disruptive meaning: the daemon and owned Geph LaunchAgent may continue. A separately confirmed tray uninstall publishes terminal intent before drain, prevents watchdog PF re-arm and owned-Geph restart, stops new transparent accepts, clears only the private PF anchor, and gives accepted streams a bounded drain while the verified owned Geph backend remains alive. It then removes the exact owned Geph LaunchAgent, private runtime, and Keychain entry. Only after that user cleanup succeeds does a one-shot PID-scoped privileged launchd worker stage the validated app bundle out of `Applications`, revalidate the exact tray PID, delete it, and remove its own label on every exit path. `nohup` is not a supported detach mechanism for the administrator AppleScript context. A cancelled prompt or failed Geph cleanup does not commit app removal. Cleanup reports failure when a verified daemon survives its bounded stop. External Geph, DNS, proxy, PAC, VPN, and PF state are never touched. | Active |
| 2026-07-13 | Root-daemon installation is transactional: enable and bootstrap must be followed by a fresh owned status, exact listener identity, and matching private-PF state. Any failure disables the launchd label, removes the plist and owned runtime, clears only `com.apple/slipstream`, and releases the owned PF token; if token release fails, its record is retained and cleanup fails visibly. An absent or disabled launchd override is durable stop intent: startup and the watchdog never reinstall or re-enable it; only an explicit `Restart Proxy` action may do so. Detached cleanup may signal only a listener PID whose command resolves to the installed Slipstream daemon; an unknown listener is never touched. | Active |
| 2026-07-11 | Diagnostics may distinguish `sidecar_only` when Slipstream's root daemon is absent but its own user Geph LaunchAgent remains loaded. This is a lifecycle fact, not a claim about PF; it contains no PID, paths, hostnames, or secrets. | Active |
| 2026-07-11 | Packaged lifecycle qualification installs the frozen daemon from the built `.app` and preserves an independent PF connection/state through reinstall, restart, and uninstall. Cross-version rollback may begin only from the first release that passes this safety gate; pre-anchor releases are not executable rollback fixtures. | Active |
| 2026-07-11 | World-readable daemon status uses privacy-bounded StatusV2. It exposes aggregate route, backend, environment, and recovery state, never hostname, IP, secret, or raw failure evidence. The tray accepts V1 and V2 during the transition release. | Active |
| 2026-07-18 | Geph exit locations are selectable only from a cached or live city catalog. When neither is available, the tray shows an explicit unavailable state and never invents country-level exits. Saving an account, enabling Geph, and an explicit proxy restart each retrigger bounded catalog discovery. Generated LaunchAgent artifacts must parse before use. The launcher retains stdout/stderr in owner-only files, checks them independently of the tray, rotates each current file after 1 MiB into one owner-only 256 KiB previous tail, and keeps the Geph PID unchanged through `exec`. Diagnostic export reads at most the final 128 KiB of either file before applying its line bound and redaction. | Active |
| 2026-07-11 | A geo-exit connection that closes before delivering any payload is a backend failure even if SOCKS CONNECT succeeded. Slipstream pauses only `com.apple/slipstream` before the next client retry; it does not change DNS, proxies, VPNs, or local-bypass routing. | Superseded 2026-07-19: only the failed geo backend cools down |
| 2026-07-12 | An abnormal client-side transport abort or failed downstream write after a local stream pause demotes only the exact generic strategy. A lone orderly EOF remains non-actionable, but two client-first orderly EOFs after at least 15 seconds without downstream progress for the same unknown host within five minutes may take the same exact-host recovery. The next retry may query Xbox DNS directly through its verified DoH endpoint and try the result locally with plain TLS; it also requires two matching clean-EOF signals before being discarded. Discord, YouTube, and other protected classes stay outside this path; it changes no macOS DNS setting and never learns or routes the host through Geph. | Active |
| 2026-07-14 | Every transparent backend relay must settle when either direction completes; direct, local bypass, Smart DNS, and Geph cannot retain a half-open peer task indefinitely. If the client ends first, record the client-first outcome before cancellation; if the server ends first, do not treat cancellation of the client pump as a clean EOF. This lifecycle rule changes no route classification or backend selection. | Active |
| 2026-07-14 | The daemon reserves emergency file descriptors and pauses only `com.apple/slipstream` when a bounded high watermark or `EMFILE`/`ENFILE` is observed. It remains dormant until usage falls below a lower watermark, then may pass the normal local-capacity gate. Resource pressure must never leave global TCP/443 captured or trigger global PF, DNS, proxy, PAC, or VPN changes. | Active |
| 2026-07-12 | Local-bypass health canaries must use a modern TLS payload handshake with the same scoped fake/desync strategy as a real client. They must not treat rejection of an intentionally minimal synthetic ClientHello as service degradation. Payload timeouts must accommodate measured endpoint latency; this affects health evidence only and never moves Discord or YouTube to Geph. | Active |
| 2026-07-11 | Manual release builds publish only uniquely tagged GitHub prereleases. They are manual-download artifacts and must not replace the stable `releases/latest` updater feed. Stable releases require an exact `v<version>` tag. | Active |
| 2026-07-15 | Every external GitHub Action used by CI and release workflows is registered and pinned to a reviewed immutable commit with a human-readable version label. JavaScript actions use Node 24 releases, JavaScript build jobs use the supported Node 24 LTS instead of EOL Node 20, and the Rust toolchain action is pinned while retaining `stable` as its explicit input default. macOS release jobs install only missing `protoc`, `cmake`, or `pkg-config` formulas with Homebrew auto-update disabled, propagate installation failures, and verify every command afterward; build prerequisites must never be hidden behind `|| true`. | Active |
| 2026-07-15 | Only stable Slipstream app releases may own GitHub's `latest` pointer and updater feed. Preview app releases use unique build-numbered names and never become latest. Vendored Geph artifacts are internal dependency prereleases, never app releases or latest releases, and publishing workflows run with write permissions only from `main`, scheduled runs, or explicit manual dispatch. Repository Actions may create the dependency update PR, but the workflow never approves or merges it; normal `main` protection and required checks still apply. | Active |
| 2026-07-16 | Until Developer ID signing, hardened runtime, notarization, and stapling are implemented, a stable `v*` tag event fails before checkout and cannot publish. Preview publication remains available from `main`. Legacy `v0.1.1` through `v0.1.4` stay immutable and non-prerelease only to preserve the existing `releases/latest` updater endpoint; their titles and notes mark them archival and they are not used for new installs. | Active |
| 2026-07-15 | Python runtime, test, and PyInstaller build graphs are separate, fully pinned lock files generated on Python 3.13 by pinned `pip-tools`. CI, release, and legacy source-install paths install only hash-verified wheels with `--require-hashes` and `--only-binary=:all:`; CI and release jobs select Python 3.13.14. Dependency changes begin in reviewed `.in` files and regenerate all three locks together. | Active |
| 2026-07-15 | macOS application builds target `aarch64-apple-darwin` explicitly. Every published app release carries a deterministic SPDX 2.3 SBOM and a strict artifact manifest bound to the source commit, source commit timestamp, target, byte sizes, and SHA-256 digests. Preview, stable, and `geph-vendor-*` tag namespaces remain distinct; release notes combine a short channel/artifact preface with GitHub-generated merged changes. Existing tags are retained and never repurposed. | Active |
| 2026-07-16 | App releases are dispatched only from `main` or an exact stable tag. After lifecycle and payload verification, GitHub Actions signs SLSA provenance for every release asset and binds the SPDX 2.3 inventory to each app archive or image using the `build-app.yml` OIDC identity. The workflow verifies the signer, source commit, predicate type, and GitHub-hosted runner before publication. | Active |
| 2026-07-16 | Dependency auditing operates on Cargo metadata resolved for the explicit release target rather than every package present in `Cargo.lock`. Pull requests, `main`, scheduled checks, and release builds use a checksum-pinned OSV Scanner. Informational advisories are recorded; every other finding blocks unless an exact package, version, and advisory exception is reviewed and unexpired. Every public audit report is bound to its SBOM, policy, source commit, target, and scanner. | Active |
| 2026-07-16 | A vendored Geph version is identified by its canonical crates.io archive SHA-256, reviewed features and macOS targets, committed `Cargo.lock`, and immutable release revision. Upstream discovery may only open a source-contract PR; binaries are built after merge with `--locked`. Every `geph-vendor-<version>-r<revision>` release publishes a full transitive SPDX inventory and fail-closed audit, plus provenance and SBOM attestations verified again by the app build. Existing tags are never replaced. Audit exceptions are exact, short-lived reachability decisions; incomplete TLS name-constraint enforcement remains explicitly accepted residual risk until upstream updates, not an unreachable-code claim. | Active |
| 2026-07-11 | Preview releases omit remote policy trust keys and signed policy artifacts. Remote policy remains disabled; a stable release must supply and verify the signed policy channel. | Active |
| 2026-07-11 | A preview is offered for installation only after the release workflow runs the privileged PF lifecycle sentinel against that exact built `.app` on a disposable GitHub macOS runner. | Active |
| 2026-07-12 | The tray watchdog does not kickstart a newly installed daemon until it has seen one fresh status snapshot or a bounded startup grace expires. This prevents a healthy daemon from being restarted while it is publishing its first StatusV2 snapshot. | Active |
| 2026-07-12 | A missing durable PF enable token plus a definitively disabled PF state proves an in-memory-only token is stale after owned recovery. Slipstream may clear only that memory and acquire a new private token; persisted, enabled, or indeterminate token states remain fail-closed. | Active |
| 2026-07-12 | Routing changes must pass deterministic data-plane traffic contracts through the real daemon handler. Each contract asserts the intended backend, prohibited backends, and first-plus-streamed payload delivery; it does not mutate PF, DNS, proxy, PAC, VPN, or the live network. | Active |
| 2026-07-12 | A fresh, user-managed Xbox DNS payload proof may be the first geo-exit backend for OpenAI or Anthropic. A runtime Smart DNS miss falls back to owned Geph only when it is verified ready; otherwise only the private PF anchor pauses for a native retry. It never falls into local desync, and Slipstream does not change the system DNS configuration. | Superseded 2026-07-19: missing app backends preserve the original system destination without pausing local routing |
| 2026-07-12 | Raw Telegram MTProto DC traffic is an unmodified safety passthrough and failure signal, not a direct-connect guarantee. When raw MTProto is blocked, the supported user path is the bundled local `tg-ws-proxy`; Telegram never routes through Geph. | Active |
| 2026-07-13 | `billing.openai.com` remains a geo-exit route, but it is not a health canary. A synthetic probe can be refused by an edge or exit without proving that the primary OpenAI flow or Slipstream is unavailable. | Active |
| 2026-07-13 | Tray recovery copy names the affected access class (`local`, `external`, or both) and states that recovery is in progress. It does not surface generic `Needs attention`; raw failure detail remains only in sanitized diagnostics. | Active |
| 2026-07-13 | A live but stale owned Geph may be restarted by the daemon only after new owned-Geph sessions are blocked and active owned-Geph sessions reach zero. Restart preparation cools only the owned geo backend; the private PF anchor and independent local bypass remain active. The daemon must verify the ownership file's user ID against its filesystem owner, the exact `dev.slipstream.geph` LaunchAgent label, and the listener PID/executable/config identity before calling `launchctl kickstart`; unknown listeners and external Geph are never touched. | Active |
| 2026-07-18 | Raw daemon logs and every retained rotation are root-owned regular files with mode `0600`; creation must not follow symlinks. `Open Status` is the passwordless default and opens a sanitized owner-only snapshot without copying the raw daemon log. `Copy Diagnostics` may make a temporary owner-only raw-log copy only after an explicit administrator prompt and redacts it before export. Every daemon process writes a session-start marker so retained failures are not presented as current. | Active |
| 2026-07-13 | Routing policy and recovery behavior shared across implementations is specified by versioned language-neutral JSON vectors under `contracts/`. Contract v1 is append-only; incompatible behavior changes require a new version so adapters can migrate deliberately. | Active |
| 2026-07-14 | Address racing and route circuit breaking are pure, versioned contracts before they enter any transport adapter. Address attempts use stable family interleaving, a shared deadline, bounded concurrency, and deterministic winner/cancellation rules. Circuit state is scoped to `(service_group, route_class, backend_id)`; opening it suppresses only that candidate and never changes policy, PF, DNS, proxy, PAC, or VPN state. Protected local-bypass groups reject a Geph circuit key. | Active |
| 2026-07-14 | Connection-race orchestration is a pure command/event state machine. It circuit-gates a route before emitting a resolver command, counts an exhausted multi-address race as one logical circuit failure, and ignores late resolver/socket events after a terminal result. A wake delivered exactly at the shared deadline defers timeout by one deterministic turn so a completion timestamped at that deadline can win regardless of queue order; later success is rejected. Scripted adapters execute the shared Python/Rust contract without network I/O; runtime adapters remain a separate change. | Active |
| 2026-07-15 | The Python connection-race I/O adapter owns every resolver task, connector task, timer, and opened stream until one winner is explicitly transferred to its caller. Production `tproxy.py` reaches it only through `connection_probe.py`, after policy and backend selection, and races the existing complete first-payload dialers for local, app-owned Xbox DNS, or freshly proven Smart DNS addresses. It never races route classes or promotes a failure to Geph. Runtime circuit state was deliberately one-shot in this first integration, so one request could not suppress a later request. Cancellation and terminal cleanup close all non-winning streams; PF, launchd, system DNS, proxy, PAC, and VPN behavior are unchanged. | Superseded by the bounded registry below |
| 2026-07-15 | Persistent route-circuit state is applied only to complete backend outcomes, never to an individual IP or desync strategy. One exhausted protected local ladder counts as one `local_engine` failure; freshly proven Smart DNS and verified owned Geph use separate geo-exit keys. An owned-Geph half-open probe recovers as soon as its first downstream payload is delivered, so a healthy long-lived WebSocket cannot retain the sole permit; an early close with zero payload remains failure. Unknown and direct traffic, external Geph, and individual Xbox DNS/local address attempts do not persist suppression. The registry is bounded to 256 non-default entries with a five-minute idle TTL and deterministic LRU; eviction can only retry the same fixed backend, never change route class. Internal registry corruption fails open by clearing suppression, while protected Discord/YouTube keys still reject every Geph edge. | Active |
| 2026-07-13 | Installed lifecycle qualification may suspend and signal only a PID whose command exactly matches the installed daemon. Suspend/resume must exercise the daemon's real wake-gap detection. The diagnostic network-change signal only queues a bounded event; all PF, backend, canary, and status work stays in the normal monitor thread. These stimuli are disposable-CI diagnostics, not user recovery commands, and never mutate DNS, interfaces, proxy, PAC, VPN, or external PF anchors. | Active |
| 2026-07-13 | Packaged tray-crash qualification runs only on disposable CI. It starts the embedded tray as the original non-root user and signals it only when UID and exact executable path match. Fresh HTTPS probes inherit no external proxy environment; the daemon PID, private PF anchor, unrelated PF anchor/state, and persistent connection must survive tray start, crash, restart, and stop. | Active |
| 2026-07-13 | Browser-restart qualification uses the runner's exact Google Chrome executable as the original non-root user with a new owner-only profile for every request. Test-local flags disable Chrome proxy use, background traffic, and QUIC so the request exercises TCP/443 through the qualified PF path; no system DNS, proxy, PAC, VPN, or browser profile is changed. | Active |
| 2026-07-13 | Safari restart qualification uses the system SafariDriver only on disposable CI. The driver runs as the original user on an explicit IPv4 loopback port; the harness uses a direct localhost control connection that cannot inherit proxy settings. Before each request the harness requires no existing Safari process, then verifies the new process by exact UID and executable path. A successful page must report `h2` or `http/1.1`; HTTP/3 cannot prove the qualified TCP/PF path and is rejected without globally blocking QUIC. The isolated WebDriver session is deleted on success or failure; a surviving browser may be signalled only through that verified PID so the next stage proves a fresh process. The harness never enables WebDriver, starts Safari, or changes Safari preferences on a workstation, and it never uses the user's normal browser profile. | Active |
| 2026-07-13 | Account-backed owned-Geph qualification is a protected, main-only manual workflow, never a pull-request secret. It runs as the login user on a disposable macOS runner with the root daemon absent and durably disabled. The harness creates only the exact Slipstream Keychain item and private user runtime, proves a real Steam HTTPS payload through the verified `:9954` listener, crashes the tray, then kills only the revalidated owned Geph PID and requires LaunchAgent `KeepAlive` to replace it. An unrelated `:9909` listener must survive. Cleanup removes only those user-owned artifacts and must leave PF, DNS, proxy, PAC, VPN, and interfaces untouched. | Active |

## Notes

- `docs/ROUTING_RESEARCH.md` records supporting investigations and references.
- `docs/TROUBLESHOOTING.md` records operational checks for repeated symptoms.
- Root README files should stay short and user-facing.
